spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:///home/locvb/Documents/microservices-demo/config-server-repository
          clone-on-start: true
      fail-start: true